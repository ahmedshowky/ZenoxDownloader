<Window x:Class="ZenoxDownloader.TrimVideoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trim Video" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Trim Video" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Start Time:" VerticalAlignment="Center"/>
            <TextBox x:Name="StartTimeTextBox" Grid.Column="1" Padding="5" ToolTip="Format: hh:mm:ss or seconds (e.g., 00:01:30 or 90)"/>
        </Grid>
        
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="End Time:" VerticalAlignment="Center"/>
            <TextBox x:Name="EndTimeTextBox" Grid.Column="1" Padding="5" ToolTip="Format: hh:mm:ss or seconds (e.g., 00:02:45 or 165)"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Leave End Time blank to trim until the end.&#x0a;Format: hh:mm:ss (e.g., 00:01:30) or seconds (e.g., 90)" 
                   Foreground="Gray" FontSize="12" TextWrapping="Wrap"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="80" Height="30" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Trim" Width="80" Height="30" Background="#3498DB" Foreground="White" FontWeight="Bold" Click="TrimButton_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
